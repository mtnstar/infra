---
- name: Include PowerDNS.pdns role
  include_role:
    name: PowerDNS.pdns

- name: Wait for PowerDNS service to be ready
  wait_for:
    port: "{{ pdns_port | default(5300) }}"
    host: 127.0.0.1
    timeout: 30
    delay: 2
