---
driver:
  name: podman
platforms:
  - name: instance
    image: ghcr.io/mtnstar/infra-test-ubuntu:24.04
    pre_build_image: true
verifier:
  name: testinfra
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "../../.galaxy/roles:../../roles"
    ANSIBLE_INVENTORY_IGNORE_EXTENSIONS: ".conf,.key,.pem"
    ANSIBLE_INVENTORY_IGNORE_PATTERNS: "wireguard_configs,wg*.conf*"
  inventory:
    hosts:
      all:
        hosts:
          instance:
            #task_list: ["1_install", "2_server-config"] 
            ansible_user: test-admin
            bootstrap_user: test-bootstrap
            timezone: "Europe/Zurich"
